# Uncomment the next line to define a global platform for your project
platform :ios, '12.1'

target 'Mobius Counter' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'MobiusCore', '0.1.0-alpha'
  pod 'MobiusExtras', '0.1.0-alpha'

  # Pods for Mobius Counter

  target 'Mobius CounterTests' do
    inherit! :search_paths
    pod 'MobiusNimble', '0.1.0-alpha'
    pod 'MobiusTest', '0.1.0-alpha'
    # Pods for testing
  end

  target 'Mobius CounterUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
